# IntelliSense 错误修复 - 快速指南

## ✅ 已修复配置

`.vscode/c_cpp_properties.json` 已更新为正确的工具链路径：
```
C:/ST/STM32CubeCLT_1.19.0/GNU-tools-for-STM32/
```

## 🔄 激活修复

**方法1: 重启 VS Code**（推荐）
- 关闭所有窗口
- 重新打开项目

**方法2: 重置 IntelliSense**
1. 按 `Ctrl+Shift+P`
2. 输入 `Reset IntelliSense`
3. 选择 `C/C++: Reset IntelliSense Database`

**方法3: 重新加载窗口**
1. 按 `Ctrl+Shift+P`
2. 输入 `Reload Window`

## ✅ 验证修复成功

打开 `Core/App/main.c`，检查：
- [ ] `#include "usart.h"` 无红色波浪线
- [ ] `printf(...)` 函数无 "undeclared" 警告
- [ ] IntelliSense 可以跳转到 `stdio.h` 定义（Ctrl+点击）

## 🔧 根本原因

**问题**: VS Code 配置的编译器路径过时  
**旧路径**: `C:/Users/Wzl/AppData/Local/stm32cube/...`  
**新路径**: `C:/ST/STM32CubeCLT_1.19.0/...`

**解决**: 手动更新 `.vscode/c_cpp_properties.json`

## 📚 详细文档

查看完整故障排查指南：
```
Docs/IntelliSense配置修复指南.md
```

## 🚀 自动化脚本（可选）

未来工具链路径变更时，可运行：
```powershell
.\scripts\update_vscode_config.ps1
```

**注意**: 脚本需要管理员权限或执行策略调整。

---

**更新时间**: 2025-12-01  
**状态**: ✅ 已修复
