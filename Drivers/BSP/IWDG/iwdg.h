/**
 ****************************************************************************************************
 * @file        iwdg.h
 * @author      正点原子团队(ALIENTEK)
 * @version     V1.0
 * @date        2025-12-01
 * @brief       独立看门狗(IWDG)驱动头文件
 ****************************************************************************************************
 * @attention
 *
 * 实验平台:正点原子 M48Z-M3最小系统板STM32F103版
 *
 * IWDG特点:
 * - 独立的32KHz RC振荡器（LSI），不依赖主时钟
 * - 一旦启动无法关闭，只能通过系统复位停止
 * - 溢出时间 = 预分频值 * 重载值 / 32KHz
 * - 超时范围: 0.1ms ~ 26214ms（约26秒）
 *
 ****************************************************************************************************
 */

#ifndef __IWDG_H
#define __IWDG_H

#include "stm32f1xx_hal.h"

/* IWDG句柄 */
extern IWDG_HandleTypeDef g_iwdg_handle;

/* 函数声明 */
void iwdg_init(uint8_t prescaler, uint16_t reload);
void iwdg_feed(void);

#endif /* __IWDG_H */
